  <%= form_for @game, :url => {:action => "throw"}, :method => "post" do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h4> Select Signal: </h4> <br />
    <%= f.select :user_signal, options_for_select(['Rock', 'Paper', 'Scissors']) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
